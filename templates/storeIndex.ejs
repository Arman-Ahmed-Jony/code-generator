import { defineStore } from 'pinia'
import actions from './actions'

export const use<%=entityName %>Store = defineStore('<%=entityName %>s', {
  state: () => ({
    <%=entityName %>s: [],
    <%=entityName %>: {}
  }),
  actions
})